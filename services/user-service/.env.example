# MongoDB Connection String
MONGODB_URI=mongodb://localhost:27017/migratio_user_service

# Service Port
USER_SERVICE_PORT=3001

# JWT Configuration
JWT_SECRET=your-very-secure-jwt-secret-replace-me
JWT_EXPIRES_IN=90d
JWT_COOKIE_EXPIRES_IN=90

# Add URLs for other services if needed (e.g., Profile Service if separate)
# PROFILE_SERVICE_URL=http://localhost:xxxx

# Email Configuration (if sending verification/reset emails from here)
# EMAIL_HOST=
# EMAIL_PORT=
# EMAIL_USER=
# EMAIL_PASS=
# EMAIL_FROM=

# CORS Origin (Frontend URL)
# CORS_ORIGIN=http://localhost:3000
