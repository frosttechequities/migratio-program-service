# MongoDB Connection String
MONGODB_URI=mongodb://localhost:27017/migratio_program_service

# Service Port
PROGRAM_SERVICE_PORT=3002

# Elasticsearch Configuration (Optional - if used for search)
# ELASTICSEARCH_HOST=http://localhost:9200
# ELASTICSEARCH_INDEX_PROGRAMS=migratio_programs

# Add URLs for other services if needed
# COUNTRY_SERVICE_URL=http://localhost:xxxx # If country data is managed separately

# CORS Origin (Frontend URL or API Gateway URL)
# CORS_ORIGIN=http://localhost:3000
